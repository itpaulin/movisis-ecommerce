services:
  server:
    build:
      context: .
    environment:
      DATABASE_URL: postgres://postgres.mjbjtcofhfovnsfujwcp:iXQG4ybtJbw00xOk@aws-0-sa-east-1.pooler.supabase.com:6543/postgres
    ports:
      - 3000:3000
    develop:
      watch:
        - path: ./package.json
          action: rebuild
        - path: ./next.config.js
          action: rebuild
        - path: ./pnpm-lock.json
          action: rebuild
        - path: .
          target: /app
          action: sync
      
volumes:
  ecommerce:
